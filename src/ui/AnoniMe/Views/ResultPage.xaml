<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AnoniMe.Views.ResultPage"
    xmlns:local="using:AnoniMe.Views"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:AnoniMe.ViewModels"
    xmlns:models="using:AnoniMe.Models"
    x:DataType="vm:ResultViewModel"
    mc:Ignorable="d">

    <StackPanel Padding="40" Spacing="20">

        <!-- 成功訊息 -->
        <TextBlock Text="去識別化完成囉！" FontSize="24" FontWeight="Bold" Foreground="#66CC33" HorizontalAlignment="Center"/>

        <!-- 預覽清單 -->
        <ItemsControl ItemsSource="{x:Bind ViewModel.PreviewFiles}">
            <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="models:PreviewFileInfo">

                    <Border Background="#F8F8F8" Padding="20" Margin="10" CornerRadius="10">
                        <StackPanel Spacing="12">

                            <!-- 檔案資訊 -->
                            <TextBlock Text="{x:Bind FileName}" FontSize="16" FontWeight="Bold"/>
                            <TextBlock Text="{x:Bind FileType}" FontSize="14"/>
                            <TextBlock Text="{x:Bind FileSizeDisplay}" FontSize="14"/>

                            <!-- PDF 預覽 -->
                            <Border Visibility="{x:Bind IsPdf, Converter={StaticResource BoolToVisibilityConverter}}" Background="White" Height="300">
                                <WebView2 Source="{x:Bind PdfUri}" />
                            </Border>

                            <!-- Word 預覽 -->
                            <ScrollViewer Visibility="{x:Bind IsDoc, Converter={StaticResource BoolToVisibilityConverter}}">
                                <TextBlock Text="{x:Bind PreviewText}" TextWrapping="Wrap" FontSize="14"/>
                            </ScrollViewer>

                        </StackPanel>
                    </Border>

                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- 檔名清單（可選） -->
        <ItemsControl ItemsSource="{x:Bind ViewModel.ProcessedFileNames}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" FontSize="14" Foreground="Gray"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- 功能按鈕群 -->
        <StackPanel Orientation="Horizontal" Spacing="20" HorizontalAlignment="Center">
            <Button Content="下載結果" Command="{x:Bind ViewModel.DownloadCommand}" />
            <Button Content="返回首頁" Command="{x:Bind ViewModel.GoHomeCommand}" />
        </StackPanel>

    </StackPanel>

</Page>